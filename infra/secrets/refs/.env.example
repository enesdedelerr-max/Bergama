# Example environment variable names for local operator bootstrap.
# Copy to an untracked .env file and populate via a secret manager.
# Never commit real values.

PLATFORM_ENVIRONMENT=paper
POSTGRES_USERNAME=
POSTGRES_PASSWORD=
REDIS_PASSWORD=
MINIO_ACCESS_KEY=
MINIO_SECRET_KEY=
ARGOCD_ADMIN_PASSWORD=
