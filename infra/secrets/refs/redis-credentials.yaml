apiVersion: v1
kind: Secret
metadata:
  name: redis-credentials
  namespace: platform
  labels:
    app.kubernetes.io/part-of: platform-foundation
    bergama.io/secret-ref: "true"
type: Opaque
stringData:
  password: "${SECRETREF:redis.password}"
