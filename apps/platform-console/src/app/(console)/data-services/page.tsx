import { ModuleUnavailable } from "@/components/common/module-unavailable";

export default function DataServicesPage() {
  return (
    <ModuleUnavailable
      title="Data Services"
      description="Data services detail views are not enabled in this shell release."
    />
  );
}
