# Bergama Strategy SDK

Author-facing contracts for strategy plugins (Sprint 4 #406).

Strategy authors depend only on the stable public API exported from `bergama_strategy_sdk`.
Runtime orchestration lives in `apps/api/app/strategy/`.
